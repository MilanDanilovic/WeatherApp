import Lottie from "lottie-react";
import noPage from "../assets/noPage.json";
const NoPage = () => {
  return (
    <div className="no-page">
      <Lottie animationData={noPage} />
    </div>
  );
};

export default NoPage;
